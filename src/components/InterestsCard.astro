---
const { label, link, category } = Astro.props as {
  label: string;
  desc: string;
  link: string;
  category: Category;
};

type Category = "science" | "photography" | "music" | "reading" | "coding";

const styles: Record<
  Category,
  { gradient: string; pattern: string; glow: string; icon: string }
> = {
  science: {
    gradient: "from-[#0f0c29] via-[#302b63] to-[#24243e]",
    pattern:
      "bg-[radial-gradient(circle_at_top_left,rgba(255,255,255,0.05),transparent_60%)]",
    glow: "shadow-[0_0_25px_rgba(80,120,255,0.4)] group-hover:shadow-[0_0_45px_rgba(120,160,255,0.8)]",
    icon: "🌌",
  },

  photography: {
    gradient: "from-[#000428] to-[#004e92]",
    pattern:
      "bg-[linear-gradient(135deg,rgba(255,255,255,0.04)_10%,transparent_10%)_0_0/12px_12px]",
    glow: "shadow-[0_0_20px_rgba(255,200,120,0.25)] group-hover:shadow-[0_0_40px_rgba(255,220,160,0.5)]",
    icon: "📸",
  },

  music: {
    gradient: "from-[#1f4037] to-[#99f2c8]",
    pattern:
      "bg-[repeating-linear-gradient(90deg,rgba(255,255,255,0.05)_0px,rgba(255,255,255,0.05)_2px,transparent_2px,transparent_6px)]",
    glow: "shadow-[0_0_25px_rgba(255,100,130,0.35)] group-hover:shadow-[0_0_45px_rgba(255,130,160,0.7)]",
    icon: "🎵",
  },

  reading: {
    gradient: "from-[#4b3f72] via-[#6c567b] to-[#c06c84]",
    pattern:
      'bg-[url(\'data:image/svg+xml;utf8,<svg width="40" height="40" xmlns="http://www.w3.org/2000/svg"><circle cx="20" cy="20" r="2" fill="rgba(255,255,255,0.05)" /></svg>\')]',
    glow: "shadow-[0_0_25px_rgba(180,120,200,0.3)] group-hover:shadow-[0_0_45px_rgba(220,160,255,0.6)]",
    icon: "📚",
  },
  coding: {
    gradient: "from-[#3f7a2e] to-[#000000]",
    pattern:
      'bg-[url(\'data:image/svg+xml;utf8,<svg width="40" height="40" xmlns="http://www.w3.org/2000/svg"><circle cx="20" cy="20" r="2" fill="rgba(255,255,255,0.05)" /></svg>\')]',
    glow: "shadow-[0_0_25px_rgba(117, 226, 86,0.3)] group-hover:shadow-[0_0_45px_rgba(220,160,255,0.6)]",
    icon: "👨‍💻",
  },
};

const style = styles[category] ?? styles.science;
---

<div class="w-full md:w-1/3 flex flex-row m-2 p-5">
  <a
    href={link}
    class={`group relative block overflow-hidden rounded-2xl border-2 border-white/10 
    bg-linear-to-br w-full ${style.gradient}
    ${style.pattern}
    backdrop-blur-xl p-6
    transition-all duration-500 hover:-translate-y-1
    ${style.glow}`}
  >
    <div class="absolute top-4 right-4 text-5xl opacity-30 pointer-events-none">
      {style.icon}
    </div>

    <h2
      class="relative mt-5 text-3xl font-extrabold
      text-transparent bg-clip-text
      bg-linear-to-r from-white/70 via-white/90 to-white"
    >
      {label}
    </h2>
    <div
      class="flex items-center gap-2 mt-2 text-white/70 group-hover:text-white transition-all"
    >
      <span
        class="opacity-40 group-hover:opacity-100 transition-all translate-x-0 group-hover:translate-x-1"
      >
        ↗
      </span>
    </div>
    <div
      class="pointer-events-none absolute inset-0 opacity-20
      bg-[linear-gradient(to_bottom,rgba(255,255,255,0.07)_1px,transparent_1px)]
      bg-size-[100%_4px]"
    >
    </div>
  </a>
</div>
